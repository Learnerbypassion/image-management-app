services:
  face-service:
    build: ./face-service
    ports:
      - "8000:8000"
    volumes:
      - ./face-service/app:/app/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  # Redis (Phase 5 — uncomment when adding BullMQ)
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   restart: unless-stopped
