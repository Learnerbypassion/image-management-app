{
  "name": "snapfind",
  "version": "1.0.0",
  "private": true,
  "description": "Upload your event photos once. Take one selfie. Get every photo you're in.",
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:worker": "cd server && npm run dev:worker",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:worker\" \"npm run dev:client\"",
    "install:all": "cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
